import java.util.*;
public class Day14 {
    public static void main(String[] args) {
        // 49
        Scanner sc = new Scanner(System.in);
      /*
        //System.out.println("Enter the character:");
        char in;
        do {
            System.out.println("Hello");
            System.out.println("Press Y to repeat");
            in = sc.nextLine().charAt(0);
        }
        while (in == 'Y');

      */
        //50
        System.out.println("Press 1 to Add\nPress 2 to subtract");// /n for new line // /t for multiple space
        // backlash / is used for proving our input is charactr (lelo) backslash kw aage wale character

       /* int ch = sc.nextInt();
        int n;
        do {

            if (ch == 1) {
                System.out.println("Enter values to be added");
                int a = sc.nextInt();
                int b = sc.nextInt();
                System.out.println(a + b);
            } else if (ch == 2) {
                System.out.println("Enter values to be sub");
                int a = sc.nextInt();
                int b = sc.nextInt();
                System.out.println(Math.abs(a - b));


            }
        }
        while (n!=0){


        }

        */
        /*
        //51
        int n;
        int sumN = 0;
        int sumPO = 0;
        int sumPEW = 0;
        do {

        }

         */
    }
}
